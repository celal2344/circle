{
  "name": "@circle/supabase",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm run check",
    "dev": "npm run check",
    "lint": "npm run check",
    "test": "npm run check",
    "typecheck": "npm run check",
    "check": "node ./scripts/check.mjs"
  }
}
